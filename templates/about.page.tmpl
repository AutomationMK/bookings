{{template "base" .}}

{{define "content"}}
<div class="container mx-auto">
    <div class="flex flex-col gap-2 px-4">
        <h1 class="text-2xl">This is the about page</h1>
        <p>This is a paragragh of text</p>
        <p>This is a paragragh of text</p>

        <p>This came from the tempate: {{index .StringMap "test"}}</p>

        <p>
            {{ if ne (index .StringMap "remote_ip") "" }}
            Your remote IP address is {{ index .StringMap "remote_ip" }}
            {{ else }}
            I don't know your ip address yet. Visit the <a href="/">Home page</a> so I can set it.
            {{ end }}
        </p>
    </div>
</div>
{{end}}
